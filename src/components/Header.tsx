import "../styles/header.scss";
import { Button } from "antd";

export const Header = () => {
  return (
    <header>
      <h2>FWW</h2>
      <Button type="link">Sign Up</Button>
    </header>
  );
};
